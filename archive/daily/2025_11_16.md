# Todo
Lovelingo
Language Learning Game

# Vim
Text highliting in vim
show 
automatically create note 
search notes using dmenu or rofi or custom command
disable relative numbers for coping text 
make # do comment 



# NGINX LOG partser: promethios loki grafana.


add dotfiles git integration dotfiles git to auto push
add diff checker to show = if nothing changed.

# Linux Admin


# look at sudo journalctl --vacuum-size=200M

analyse it and compress insights before deleting it. 

# Find big files:
sudo du -h --max-depth=1 / | sort -hr
sudo du -ah /home | sort -hr | head -n 20
sudo du -h --max-depth=1 /var | sort -hr


# i3 
mod + shift + c = reload


